/**
 * @file __mocks__/fileMock.js
 * @description Jest-Stubbing für statische Assets (z. B. Bilder). Liefert
 * einen einfachen String, den Tests als Platzhalter verwenden können.
 */

const fileMock = "file-mock";

export default fileMock;
